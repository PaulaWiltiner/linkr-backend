import { connection } from "../dbStrategy/postgres.js";

export async function getOneHashtag(id) {
  return connection.query(``);
}

export async function getHashtags(id) {
  return connection.query(``);
}
